> Socket = IP + PORT


###### netstat util
> -a show all connections
> -n show adress and ports in digit format
```
netstat -an | find "LISTENING"
```

###### ipconfig
```
ipconfig /all
ipconfig /renew
ipconfig /displaydns
ipconfig /flushdns
```

###### nslookup
